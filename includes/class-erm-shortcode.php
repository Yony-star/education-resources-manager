<?php
/**
 * Shortcode handler (stub — Agent 05).
 *
 * @package Education_Resources_Manager
 */

if ( ! defined( 'WPINC' ) ) {
	die;
}

/**
 * Class ERM_Shortcode
 */
class ERM_Shortcode {

	/**
	 * Render the recursos_educativos shortcode.
	 *
	 * @param array<string, string> $atts Shortcode attributes.
	 * @return string
	 */
	public function render( $atts ) {
		unset( $atts );
		return '';
	}
}
